# 深度研搜：让 AI 真正完成一次研究任务

## 项目简介

「深度研搜」是一套面向真实研究场景的对话式多智能体系统。

它解决的不是“让大模型回答一个问题”这么简单的事情，而是让 AI 能够围绕一个复杂目标，自动完成任务拆解、资料检索、数据查询、知识库问答、附件阅读、信息汇总与报告交付。

用户只需要提出类似下面的需求：

> 结合公开资料、业务数据库和我上传的行业报告，整理一份行业研究报告，并生成 PDF。

系统就会根据任务内容，判断需要使用哪些信息来源，调度不同能力的专家智能体协同工作，最后将分散的信息整理成可阅读、可下载的 Markdown 或 PDF 结果。

## 我们的核心思路

### 1. 用主智能体负责“思考和调度”

复杂研究任务通常包含多个不同环节，单个智能体既要搜索网页、查询数据库，又要理解私有文档，容易造成职责混乱和上下文膨胀。

因此，项目采用 **Orchestrator-Workers（调度者-执行者）** 模式：

- 主智能体负责理解用户目标、拆解任务、制定执行计划；
- 根据任务需要，主智能体将工作分派给不同的专家助手；
- 专家助手只处理自己擅长的信息来源；
- 主智能体最终汇总各路结果，形成统一答案和交付文件。

这种设计让每个智能体的职责更清晰，也让系统更容易扩展新的数据源和工具。

### 2. 让不同信息源由不同专家处理

研究任务的信息来源往往是异构的：公开网页适合搜索，业务数据适合 SQL，企业内部资料适合知识库，用户临时提供的文件则需要专门读取。

项目将这些能力拆分为三个专家助手：

| 智能体 | 负责内容 | 主要能力 |
| --- | --- | --- |
| 网络搜索助手 | 互联网公开资料 | 搜索新闻、政策、产品和行业信息 |
| 数据库查询助手 | 结构化业务数据 | 查看表结构、预览数据、执行 SQL 查询 |
| RAGFlow 助手 | 私有非结构化知识 | 查询企业内部文档和知识库 |

用户上传的 PDF、Word、Excel、Markdown 或文本文件，则由主智能体通过文件工具直接读取并纳入分析。

### 3. 从“检索信息”走到“交付结果”

项目不把目标停留在返回几段模型生成的文字，而是把研究过程完整连接起来：

```text
用户提出研究任务
        ↓
主智能体分析目标并拆解步骤
        ↓
按需调用网络搜索、数据库、知识库和文件工具
        ↓
汇总多来源信息并判断内容是否足够
        ↓
生成 Markdown 报告
        ↓
按需转换为 PDF
        ↓
前端实时展示执行过程、答案和文件
```

这使系统更接近一个真正的研究助手，而不是一次性的问答 Demo。

### 4. 让长任务过程可观察、可交互

深度研究往往需要较长时间。如果用户只能等待最终答案，就很难知道系统正在做什么，也无法定位失败原因。

项目通过 WebSocket 实时推送任务事件，包括：

- 当前正在调用的工具；
- 当前正在执行的专家助手；
- 会话目录和文件处理状态；
- 阶段性结果、最终答案和生成文件；
- 任务取消与异常信息。

前端因此可以像观察一个真实的工作流一样，实时呈现智能体的执行进度。

## 系统架构

项目由以下几层组成：

```text
React + Vite 前端
        ↕ HTTP / WebSocket
FastAPI 服务层
        ↓
DeepAgents 主智能体
   ├── 网络搜索助手 → Tavily
   ├── 数据库查询助手 → MySQL
   ├── RAGFlow 助手 → RAGFlow 私有知识库
   └── 文件与报告工具 → 附件读取 / Markdown / PDF
```

为了避免不同任务之间相互干扰，系统使用 `thread_id` 和独立的 `session_dir` 管理会话上下文与文件目录；底层结合 LangGraph 的运行时和检查点机制，为长任务执行提供清晰的状态边界。

## 项目亮点

- **一主三从的多智能体协作**：将任务规划、专业检索和结果汇总分离，职责清晰。
- **多来源信息融合**：同时支持公开网络、MySQL 结构化数据、RAGFlow 私有知识库和用户上传附件。
- **真正可交付的研究结果**：支持生成 Markdown，并按需转换为 PDF 文件。
- **完整的前后端闭环**：覆盖任务启动、文件上传、实时进度、任务取消、结果展示和文件下载。
- **会话级上下文隔离**：不同任务拥有独立的会话身份和工作目录，降低数据串扰风险。
- **工程化而非玩具 Demo**：包含后端服务、智能体编排、工具封装、数据接入、文件处理和前端交互。
- **易于学习和扩展**：可以继续接入新的专家智能体、数据源、权限系统、任务队列和评测体系。

## 技术栈

| 模块 | 技术 |
| --- | --- |
| 智能体编排 | DeepAgents、LangGraph、LangChain |
| 大模型接入 | OpenAI 兼容接口 |
| 网络检索 | Tavily |
| 结构化数据 | MySQL |
| 私有知识库 | RAGFlow |
| 后端服务 | FastAPI、Uvicorn |
| 实时通信 | WebSocket |
| 文件处理 | pypdf、python-docx、pandas、ReportLab |
| 前端界面 | React、Vite、Ant Design、Tailwind CSS |
| 环境与依赖 | Docker Compose、uv、pnpm |

## 适合解决什么问题

「深度研搜」适合需要综合多个信息来源的研究型任务，例如：

- 整理行业趋势、竞品信息和政策变化；
- 查询业务数据库中的库存、销售或运营数据；
- 基于企业内部知识库回答专业问题；
- 阅读用户上传的研究报告并提炼摘要；
- 结合外部资料与内部资料生成专题报告；
- 将研究结果整理成 Markdown 或 PDF，直接交付给团队使用。

## 项目价值

这个项目的重点不只是展示某一个框架的 API，而是把 AI 应用从“模型调用”推进到“任务执行”：

> 从一个对话入口开始，经过多智能体规划、工具调用、异构数据检索、上下文管理和实时事件推送，最终产出可以下载和继续使用的研究成果。

对于希望学习 DeepAgents、多智能体系统和 AI 应用工程化落地的人来说，「深度研搜」提供了一条完整、清晰且可运行的实践路径；对于希望继续二次开发的团队来说，它也提供了接入更多工具和业务流程的基础骨架。

## 项目定位

当前项目聚焦于跑通深度研究的核心链路：多智能体调度、真实工具接入、文件交付、后端接口、实时通信与前端联调。

它是一个适合学习、演示和二次开发的工程化项目。用户权限、多租户、分布式任务队列、生产监控、全量审计和系统化评测等能力，可以在此基础上继续建设。

## 一句话介绍

**「深度研搜」是一套能自动规划研究任务、协同检索多源信息，并实时生成可交付报告的对话式多智能体系统。**

